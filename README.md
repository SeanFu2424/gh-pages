# 公路车 AI 教练台（GitHub Pages 部署版）

单文件静态站点，零依赖、全内联、离线可用。适合用 intervals.icu 自动拉数据 + AI 简报 + 分段统计的业余公路车训练者。

**当前版本：v1.5** ｜ 在线地址：https://seanfu2424.github.io/gh-pages/

## 一键部署到 GitHub Pages（约 2 分钟）

1. 在 GitHub 新建一个**空仓库**（不要勾选 README / LICENSE / .gitignore）。记下仓库地址，例如：
   `https://github.com/你的用户名/cycling-coach.git`

2. 本目录已经 `git init` 并完成首次提交，你只需补上远程并推送：
   ```bash
   git remote add origin https://github.com/你的用户名/cycling-coach.git
   git push -u origin main
   ```

3. 仓库页面 → **Settings → Pages** → Source 选 `main` 分支 / `(root)` → Save。
   等待约 1 分钟，访问 `https://你的用户名.github.io/cycling-coach/` 即可。

## 手机每天用
浏览器打开上面的链接 → 分享 → **添加到主屏幕**（iOS Safari / 安卓 Chrome）。
图标已内联为 PWA，会像 App 一样全屏打开，每天点开即用。

## 数据说明（重要）
- 数据存浏览器 `localStorage`（按域名隔离）。**PC 与手机是两套独立数据，不互通。**
- 换设备请用页面内的「导出 JSON 备份 / 导入恢复」迁移。
- 部署后是公开链接，任何拿到链接的人都能打开页面，但**看不到你的数据**——数据只在本机浏览器里。

## 更新
改完 `index.html` 后：
```bash
git add index.html && git commit -m "update" && git push
```
再到 Pages 设置确认已发布分支，通常自动生效（最多等 1 分钟）。

## 功能速览
- 今日训练 + 一键「拉取今日数据」（intervals.icu API 直连，CORS 已开放），旁边可「去录入」手动补录
- 一键生成 / 复制给 AI 的简报（含分段统计）
- 趋势看板（CTL/ATL/TSB）、教练档案、目标计划
- 移动端适配 + PWA，PC / 手机同源可用

## 使用动线

每天的闭环就三步，都在「今日训练」页：

1. 「今天要处理」卡右上角 → **拉取今日数据**（绿色按钮），自动同步 intervals.icu 的活动与体能数据
2. 「AI 教练简报」→ 生成 → 复制，粘给 GPT / Claude
3. 把 AI 的回复粘回「把 AI 的结论存回档案」→ 自动抽成长期记忆，下次简报会带上

漏了训练、或者不想连 intervals.icu，就点旁边的 **去录入**：直接跳到手动补录表单，填完即存。

**第一次用**：点右上角「连接设置」，填 intervals.icu 的 API Key 即可。
配置区平时收在页面最底部，不干扰日常操作。

## 更新日志

### v1.5（2026-09-11）
标识升级为「Ai」+ 第二个卡片头部对齐 + App 图标与页面统一。

- **Logo 加了 i，组成「Ai」**：左边的自行车外形本来就接近大写 A，右侧补一个同粗细的
  小写 i（圆点 + 竖笔），既代表「自己」，也点明 AI 教练。
  自行车与 i 放在同一个 `<g transform>` 里，描边宽度完全一致（2 × 0.62 = 1.24）
- **App 图标与页面 logo 彻底对齐**：此前主屏幕图标还是**旧的深青色（#0d948e）+ 另一款
  自行车造型**，和页面里的品牌绿完全不是一回事。现在三处图标全部重新生成——
  品牌绿渐变底 + 同一份 Ai 几何数据
  - `apple-touch-icon` 180×180（iOS 主屏幕）
  - `rel="icon"` 96×96（浏览器标签页）
  - manifest 图标：180×180 `any` + 512×512 `maskable`（安卓自适应）
  - 页面 SVG 与 PNG 用同一份坐标渲染，不会再出现两套视觉
- **「本次骑行」卡片头部与「今天要处理」统一**：
  - 按钮改为 `[手动补录]`（次要）+ `[复制本次骑行]`（绿色主按钮），
    左次右主，顺序和「今天要处理」一致
  - 「复制本次骑行（含分段）」去掉括号，按钮文案和绿色主按钮身份都对上了
  - 窄屏下两个按钮平分一行、各 46px 高；通过共用的 `.head-act` 类实现，
    以后再加卡片直接套这个类就行
  - 头部允许换行改为全局 `.card-h` 规则，不再只对「今天要处理」生效

### v1.4（2026-09-11）
「今天要处理」卡的操作按钮去重：只在卡片头部保留一组。

- **拉取今日数据 + 去录入 合并到卡片头部**：此前待办条目里也各有一份
  「拉取今日」「去录入」，和头部的「拉取今日数据」完全重复。现在统一收在卡片
  右上角，两个按钮并排——**拉取今日数据仍是绿色主按钮**，「去录入」为次要按钮
- **「去录入」变成真跳转**：以前只是切到今日训练页，现在会同时展开手动补录表单、
  滚动到表单位置并聚焦第一个输入框
- **去掉其余重复的同步入口**：待办「今日已记录」条目只留「生成简报」，
  原「重新同步」按钮移除；同步状态行里超过 2 天未同步时的「立即同步」按钮
  也改为纯文字提示「已 N 天未同步」（顶部就有同步按钮）
- **同步状态行文案**：重复触发按钮去掉后，状态行只做状态提示，不再承担操作
- 待办条目无按钮时不再渲染空的按钮容器（避免手机上多出一条空白）
- **手机端**：卡片头部两个按钮平分一行、各 46px 高，仍是单手可及的全宽布局

### v1.3（2026-09-11）
iPhone 适配 + 视觉换成 WorkBuddy 设计语言 + 结论存档格式可关闭。

- **iPhone 刘海屏适配**：页面顶部此前会被刘海 / 灵动岛遮挡（标题和副标题那一行）。
  现在用 `env(safe-area-inset-*)` 给上 / 下 / 左 / 右都留出安全区，
  独立添加到主屏幕后标题不会再被顶到状态栏里
- **UI 风格对齐 WorkBuddy**：品牌色 `#0d9488` → **`#28b894`**（WorkBuddy 品牌绿），
  中性色阶改用 WorkBuddy 的 `#333/#666/#808080` 与 `#f5f5f5` 底色，
  圆角从 14/10px 收紧到 8/6px，图表线色、主题色 `theme-color`、
  PWA manifest 一并同步
- **「AI 结论存档」的固定格式改为可开关**：新增开关「要求固定格式『日期：结论』」，
  关掉后不校验格式——粘什么存什么，按空段拆条、日期取今天，完整内容不截断。
  识别不到格式时，报错里直接给「关掉格式要求、按自由格式存入」一键降级按钮。
  开关状态本地保存，下次打开保持

### v1.2（2026-09-11）
两个安全 / 可感知性的改进。

- **「清空数据」收进顶栏「更多」菜单**：清空示例数据、清空全部数据 不再和
  导出 / 导入 / 连接设置 并列，避免误触；仍保留二次确认弹窗。
  点菜单外任意处或按 Esc 收起菜单
- **常驻「上次同步」时间**：「今天要处理」卡内新增一行小字，显示
  「上次同步：今天 10:12 · 共 N 条训练 / N 天体能」；
  超过 2 天未同步会转橙色提醒并出现「立即同步」按钮。
  未连接时显示「手动录入模式 · 连接 intervals.icu 后可一键同步」
- 同步时间改为记录完整时间戳（此前只记日期，看不出是今早还是昨晚拉的）

### v1.1（2026-09-11）
复制给 AI 的内容改为**纯数据输出**，不再带任何解读话术或提问指令。

- **去掉所有解读话术**：VI、EF、功率/心率解耦 三处括号注释全部移除，只留数值
- **去掉给 AI 的指令段**：「请重点判断…」「请基于以上数据分析…」不再输出，
  怎么用这份数据由你和 AI 自己决定
- **删除「历史同课对比」段落**
- **删除做功 / 热量 / 碳水三行**：intervals.icu 的 `kilojoules` 字段实为焦耳，
  显示成 kJ 会放大 1000 倍；碳水摄入为空时补 0，同样是假数据
- **分段统计只保留逐段明细**：原来的「组汇总」表来自 intervals.icu 的 `icu_groups`
  自动聚类，不是你实际做的分段（2×15min 会被聚成「2 次 ×7:28，NP 0」），已从简报中移除。
  页面上的分段统计卡片仍保留该表，供人工参考

### v1.0（2026-09-09）
首个正式版本。

- **UI 重排**：「数据源」配置区从页面顶部移到「今日训练」页最底部，并做视觉弱化；
  顶栏新增「连接设置」入口，一键跳转并展开配置面板
- **手动补录精简**：默认只显示 日期 / 名称 / 时长 / 距离 / TSS / NP 六项；
  心率、爬升、平均功率、做功、功率心率解耦 等次要项收进「更多维度」
- **页面聚焦**：「今天要处理」只在「今日训练」页显示，翻趋势 / 档案 / 目标时不再占用首屏
- **移动端**：顶栏操作按钮瘦身，导出 / 导入 / 连接设置 / 清空数据 在窄屏一行放得下
